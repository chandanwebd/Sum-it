# Sum-IT website: handoff / context for the next session

Paste the prompt below into a new chat. It gives full context of what has
already been done.

---

## PROMPT TO PASTE

You are continuing work on the Sum-IT website: a Dutch ZZP bookkeeping product by Alpha Nova B.V., domain sum-it.eu. The repo is at `D:\Rishi\Chandan\Sum-it`. Read `docs/HANDOFF.md`, `docs/ui-ux-audit.md` and `docs/blog-audit.md` first. Do not redo finished work, and preserve existing functionality, routes, APIs and SEO.

### Project facts
- **Stack:** static multi-page HTML + vanilla JS on Cloudflare Pages, plus Pages Functions in `functions/api/*` (`signup`, `stats/public`, `laposta-webhook`).
  - There is no `package.json`, no build step, no TypeScript and no lint. Validate in the browser: console errors, horizontal overflow at 320–1920px, interaction checks.
- **Backend:** Supabase.
  - Tables: `blog_posts`, `content_overrides`, `signups`, `admin_users`.
  - Admin check: RPC `is_admin()`.
  - Config: `sb-config.js`, `config.js`.
- **Languages:** NL default, EN via `/i18n.js` + `window.PAGE_I18N`. The homepage has its own `I18N` object in `js/index.js`.
  - `setLang()` replaces the innerHTML of `[data-i18n]` elements, so never put extra markup inside those elements.
- **Font:** DM Sans. **Design tokens:** `css/tokens.css`.
- **Local preview:** a small Node static server, e.g. `node serve.js <repo> 5173`, with `.claude/launch.json` pointing at it. Use absolute paths like `/components/...`, so the site must be served from its root.

### Branches (none pushed or merged yet)
`main` ← `ui-redesign` (Step 1) ← `blog-redesign` (blog work, current branch).

### DONE — Step 1: UI/UX modernization (branch `ui-redesign`)
- **Design tokens and base styles.** `css/tokens.css` defines colours, type scale, spacing, containers, radii, shadows, motion, focus ring, skip link and reduced motion.
- **Shared header** in `components/header/` (`header.html`, `header.css`, `header.js`, `sync.mjs`):
  - The markup is written into every public page between `<!-- site-header:start/end -->` markers. After editing `header.html`, run `node components/header/sync.mjs`.
  - Accessible mobile drawer. `body.has-dark-hero` makes the bar transparent over a dark hero. `data-page` on `<body>` sets the active nav item.
- **`css/site.css`:** shared stylesheet for 97 content pages (blog articles, `functies/*`, `voor/*`, `prijzen`, `over-ons`, `tools/*`, the alternatief pages, `bonnetjes-scannen`, `privacy`). Their duplicated inline CSS was removed.
- **Homepage:**
  - Bootstrap removed; `overflow-x: hidden` removed; about 880 unused CSS lines deleted.
  - Readable (AA) text on buttons; visible focus states.
  - Keyboard-operable product mockup; heading order fixed.
- **Community page:** a single shared header; Bootstrap and `css/index.css` removed.
- **Auth and signup pages** (login, wachtwoord, portal, beheer, aanmelden, aanmelden-gelukt): DM Sans font, tokens, AA contrast, labels tied to their inputs.
- **Bugs fixed:**
  - `login.html` now loads supabase-js.
  - The referral Copy button's i18n no longer breaks its "copied" state.
  - The community page's double header is gone.
- **Audit and status:** `docs/ui-ux-audit.md`.

### DONE — Blog redesign + block-based CMS (branch `blog-redesign`)
- **Landing page (`/blog/`):**
  - The static cards are generated by `scripts/build-blog-index.mjs` from `blog/index.json`. After adding or moving a static article, edit that JSON and run the script.
  - `blog/blog.js` adds:
    - a sticky category bar that also filters, with smooth scrolling;
    - search (250ms debounce);
    - Newest/Oldest sort;
    - URL state (`?category=&sort=&search=`);
    - a "latest" section and 6-card previews per category;
    - empty and error states, EN/NL;
    - CMS posts merged from Supabase.
  - Styles: `blog/blog.css`.
- **Block system:**
  - `blog/blocks.js` is the shared renderer. Block types: `rich_text` (Quill Delta), `heading`, `image`, `two_images`, `quote`, `image_text`. Stored format: `{"version":1,"blocks":[{id,type,data}]}`.
  - It builds the page safely (text only, allow-listed URLs) and skips unknown block types. New types can be added with `SumitBlocks.register()`.
- **Article page:** `blog/post.html` + `blog/post.js` (`?slug=`, and `?preview=1` for editor drafts).
  - Layout: breadcrumb, category, title, lead, byline, tags, cover, blocks, related articles, CTA.
  - Legacy `body_html` is still rendered.
- **Editor:** `beheer.html` → Blog tab, built from `blog/editor.js` + `blog/editor.css`. Quill 2.0.3 is loaded from jsdelivr with SRI.
  - Metadata, cover image, add/duplicate/move/drag/delete blocks, upload to the Supabase bucket `blog-images`, alt-text validation, live and full-page preview.
  - Draft/publish uses the `published` boolean. Saves `blocks` plus a generated `body_html`.
  - Before the migration, it saves only the old columns and shows a warning.
- **`supabase-setup-v5.sql`** (additive): adds the columns `blocks`, `category`, `cover_image`, `cover_alt`, `tags`, `author_name`, `published_at`, `meta_title`, `meta_description` and `og_image`, and creates the `blog-images` bucket with admin-only write.
- **Audit and status:** `docs/blog-audit.md`.

### PENDING / NOT DONE (needs decision or action)
1. **Owner actions:**
   - Run `supabase-setup-v5.sql` in the Supabase SQL Editor. Until then, category, cover, tags, SEO fields and uploads are unavailable, and each visitor session logs one harmless 400 error.
   - The optional privacy hardening at the end of that file hides the editor's e-mail stored in `blog_posts.author` from the public API.
2. **Admin access:** the user has no admin login yet. They can sign up at `/portal.html`, then:
   - an existing admin adds them on the Admins tab, or
   - someone with Supabase access runs `insert into public.admin_users(email, added_by) values ('<email>','manual');`
3. **Not tested live:** real sign-in, save and upload against Supabase (only a mock was used).
4. **Functional bugs left alone (need a decision):**
   - The homepage signup (`js/index.js`) and `aanmelden.html` show success even when the request fails.
   - `admin.html` `loadDefaults()` reads `I18N` from `index.html`, but it now lives in `js/index.js`. `admin.html` also has no `is_admin` check.
   - The referral link is hard-coded as `JOU4F2` in `copyRef`.
   - The homepage hero carousel ignores the reduced-motion setting.
5. **Not restyled:** `admin.html`, `demo/app.html`. `css/index.css` still has stacked legacy layers (about 9,100 lines) and needs a consolidation pass.
6. **Step 2 — SEO (next planned phase):**
   - Remove, or mark noindex, the public backups: `index copy.html`, `indexv1.html`, `community/index copy.html`.
   - Mixed `.html` vs extensionless internal links.
   - `/privacy` is missing from the sitemap.
   - Homepage has `lang="nl"` but an English title.
   - CMS posts (`post.html?slug=`) are rendered by JS only: no canonical, not in the sitemap.
   - Overlapping pages: `/bonnetjes-scannen` vs `/functies/bonnetjes-scannen/`.
   - The blog intro text says "82 artikelen"; there are 85.
7. **Git:** decide whether to merge `ui-redesign` and `blog-redesign` into `main` and push.

### Rules to keep following
- Change presentation, not business logic.
- Don't rename IDs, classes or `data-*` attributes used by JS (see the lists in `docs/ui-ux-audit.md` §3).
- Don't submit real forms or write to production Supabase without asking.
- Work incrementally: verify in the browser, then commit with a clear message.
